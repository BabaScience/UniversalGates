from gates import NAND

# Example usage
if __name__ == "__main__":
    # Test the NAND gate with all possible inputs
    print("NAND(0, 0):", int(NAND(0, 0)))
    print("NAND(0, 1):", int(NAND(0, 1)))
    print("NAND(1, 0):", int(NAND(1, 0)))
    print("NAND(1, 1):", int(NAND(1, 1)))
